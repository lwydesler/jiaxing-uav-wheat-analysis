# 结果表表头说明与阅读方法

本文档只说明各结果表的表头含义，以及应该怎么看。

## 1. field_samples_clean.csv

这个表是清洗后的地面调查数据表。每一行代表一个采样点。

| 表头 | 含义 | 怎么看 |
|---|---|---|
| sample_id | 样点编号 | 用来区分不同采样点 |
| x | 样点 X 坐标 | 表示样点位置 |
| y | 样点 Y 坐标 | 表示样点位置 |
| Flower_num | 杨花数 | 数值越大，表示该样点杨花数量越多 |
| Flower_rat | 杨花率 | 数值越大，表示该样点杨花比例越高 |
| FHB_num | 病害株数 | 数值越大，表示病害株数越多 |
| FHB_rate | 发病率 | 数值越大，表示发病比例越高 |
| FHB_index | 病害指数 | 数值越大，表示病害程度越重 |

阅读方法：

```text
看单个采样点的地面实测情况，主要看 Flower_num、Flower_rat、FHB_num、FHB_rate、FHB_index。
空白值表示该指标没有有效数据。
```

## 2. sample_rs_features.csv

这个表是遥感特征表。每一行代表一个采样点，每一列是从影像中提取的遥感特征。

| 表头形式 | 含义 | 怎么看 |
|---|---|---|
| sample_id | 样点编号 | 用来和地面调查表对应 |
| 0418_... | 4 月 18 日影像特征 | 表示该样点在 4 月 18 日影像上的遥感特征 |
| 0510_... | 5 月 10 日影像特征 | 表示该样点在 5 月 10 日影像上的遥感特征 |
| diff_... | 两期差值 | 表示 5 月 10 日减去 4 月 18 日 |
| ratio_... | 两期比值 | 表示 5 月 10 日除以 4 月 18 日 |
| rel_... | 两期相对变化 | 表示两期变化幅度 |
| mean | 平均值 | 表示采样范围内平均水平 |
| median | 中位数 | 表示采样范围内中间水平 |
| p25 | 第 25 百分位数 | 表示偏低水平 |
| p75 | 第 75 百分位数 | 表示偏高水平 |
| valid_count | 有效像元数 | 数值越大，说明参与统计的有效像元越多 |

示例：

| 表头 | 含义 |
|---|---|
| 0510_r0p172_ND_B2_B3_median | 5 月 10 日、0.172 m 采样范围内，B2 和 B3 指数组合的中位数 |
| diff_r0p172_DVI_mean | 0.172 m 采样范围内，DVI 指数从 4 月 18 日到 5 月 10 日的平均变化量 |

阅读方法：

```text
这个表主要用于查看每个样点对应的遥感特征值。
如果某个特征在后续结果中很重要，可以回到这个表查看各样点的具体数值。
```

## 3. all_features_targets.csv

这个表是完整数据表，包含地面调查指标和遥感特征。

| 表头类型 | 含义 | 怎么看 |
|---|---|---|
| sample_id、x、y | 样点编号和坐标 | 用来定位样点 |
| Flower_num、Flower_rat | 杨花指标 | 表示地面实测杨花情况 |
| FHB_num、FHB_rate、FHB_index | 病害指标 | 表示地面实测病害情况 |
| 0418_...、0510_...、diff_...、ratio_...、rel_... | 遥感特征 | 表示对应样点的影像特征 |

阅读方法：

```text
如果想同时查看某个样点的地面实测值和遥感特征值，就看这个表。
这个表列数较多，通常用于数据追溯，不适合直接作为汇报主表。
```

## 4. correlation_summary.csv

这个表是相关性分析结果表，用来说明遥感特征和地面指标之间的关系。

| 表头 | 含义 | 怎么看 |
|---|---|---|
| target | 地面指标 | 表示正在分析哪个指标，如 Flower_rat 或 FHB_index |
| feature | 遥感特征 | 表示和该地面指标进行相关分析的遥感变量 |
| n | 有效样本数 | 数值越大，结果越稳定 |
| pearson_r | Pearson 相关系数 | 看线性相关关系，正值为正相关，负值为负相关 |
| pearson_p | Pearson 显著性 | 数值越小，线性相关越显著 |
| spearman_r | Spearman 相关系数 | 看整体高低趋势关系，建议优先看这一列 |
| spearman_p | Spearman 显著性 | 数值越小，趋势相关越显著 |
| abs_pearson_r | Pearson 相关系数绝对值 | 用于排序，数值越大表示线性关系越强 |
| abs_spearman_r | Spearman 相关系数绝对值 | 用于排序，数值越大表示趋势关系越强 |

阅读方法：

```text
1. 先看 target，找到关心的地面指标。
2. 再看 feature，了解哪个遥感特征与它关系最强。
3. 优先看 spearman_r，判断趋势关系强弱和方向。
4. spearman_r 为正，表示遥感特征越高，地面指标总体越高。
5. spearman_r 为负，表示遥感特征越高，地面指标总体越低。
6. 再看 spearman_p，数值越小，说明这个关系越可靠。
```

## 5. model_metrics.csv

这个表是模型效果评价表，用来说明遥感特征预测地面指标的效果。

| 表头 | 含义 | 怎么看 |
|---|---|---|
| target | 预测目标 | 表示模型预测的是哪个指标 |
| model | 模型名称 | 表示使用了哪种模型 |
| cv | 交叉验证方式 | 表示模型效果是如何验证的 |
| n | 有效样本数 | 表示参与建模的样本数量 |
| n_features | 特征数量 | 表示模型使用了多少个遥感特征 |
| r2 | 模型解释能力 | 越接近 1 越好；接近 0 表示预测能力较弱；小于 0 表示效果较差 |
| rmse | 均方根误差 | 预测误差，越小越好 |
| mae | 平均绝对误差 | 平均预测偏差，越小越好 |
| pearson_r | 预测值和实测值的线性一致性 | 越接近 1 越好 |
| spearman_r | 预测值和实测值的趋势一致性 | 越接近 1 越好 |
| features | 模型使用的特征 | 表示模型输入了哪些遥感特征 |

阅读方法：

```text
1. 先看 target，找到关心的预测指标。
2. 再看 model，比较不同模型。
3. 优先看 r2，判断模型整体预测能力。
4. 再看 rmse 和 mae，判断预测误差大小。
5. 再看 spearman_r，判断模型能否反映高低趋势。
6. 如果 r2 不高，但 spearman_r 还可以，说明模型更适合判断高低趋势，不适合精确预测数值。
```

## 6. feature_importance.csv

这个表是模型特征重要性表，用来说明模型主要依赖哪些遥感特征。

| 表头 | 含义 | 怎么看 |
|---|---|---|
| target | 预测目标 | 表示模型预测的是哪个地面指标 |
| model | 模型名称 | 表示是哪一个模型的结果 |
| feature | 遥感特征 | 表示参与模型预测的遥感变量 |
| importance | 特征重要性 | 数值越大，说明模型越依赖这个特征 |

阅读方法：

```text
1. 先看 target，找到关心的指标。
2. 再看 model，确认是哪种模型。
3. 按 importance 从大到小看。
4. 排名前面的 feature，是模型认为更重要的遥感特征。
```

注意：

```text
importance 不是相关系数。
它表示模型在综合多个特征时，对某个特征的依赖程度。
```

## 7. 推荐阅读顺序

```text
1. field_samples_clean.csv：先了解地面实测数据。
2. correlation_summary.csv：看哪些遥感特征和地面指标关系最强。
3. model_metrics.csv：看模型预测效果好不好。
4. feature_importance.csv：看模型主要依赖哪些遥感特征。
5. all_features_targets.csv：需要追溯单个样点时再看。
6. sample_rs_features.csv：需要查看遥感特征原始值时再看。
```
