# 遥感影像与扬花及病害指标关系分析实验思路及结果整理

## 1. 研究目标

本实验的核心目标是分析无人机多光谱遥感影像与田间调查指标之间的关系，并评估是否可以利用遥感影像对扬花和病害指标进行预测。

研究对象包括两类田间指标：

- 扬花指标：`Flower_num` 和 `Flower_rat`，分别表示扬花数和扬花率。
- 病害指标：`FHB_num`、`FHB_rate` 和 `FHB_index`，分别表示病株数、发病率和病害指数。

田间采样数据对应面积为 1 平方尺。由于无人机影像空间分辨率约为 0.0575 m，1 平方尺约对应 28 个像素面积，因此正式分析中重点使用 1 平方尺等面积邻域，同时保留点采样和更大缓冲区作为尺度效应对照。

## 2. 数据与波段

遥感数据包括两期无人机多光谱影像：

- 4月18日影像：用于表示较早生育阶段的冠层光谱状态。
- 5月10日影像：用于表示接近扬花和病害观测时期的冠层光谱状态。

本次分析没有使用校正后的 4月18日影像，而是基于原始对齐后的两期影像开展关系分析。

多光谱影像共有 6 个波段，顺序为：

| 波段 | 含义 |
|---|---|
| B1 | Blue，蓝光 |
| B2 | Green，绿光 |
| B3 | Red，红光 |
| B4 | RedEdge1，红边1 |
| B5 | RedEdge2，红边2 |
| B6 | NIR，近红外 |

田间样点数据中，不是每个样点都有全部指标。各指标有效样本量如下：

| 指标 | 有效样本数 | 最小值 | 中位数 | 最大值 | 正值样本数 |
|---|---:|---:|---:|---:|---:|
| Flower_num | 33 | 1.000 | 23.000 | 30.000 | 33 |
| Flower_rat | 33 | 3.330 | 76.670 | 100.000 | 33 |
| FHB_num | 63 | 0.000 | 1.000 | 21.000 | 34 |
| FHB_rate | 62 | 0.000 | 1.580 | 37.740 | 34 |
| FHB_index | 62 | 0.000 | 0.005 | 0.380 | 31 |

从样本分布看，扬花指标有效样本量较少，只有 33 个；病害指标样本量相对多一些，约 62-63 个。病害指标中 0 值较多，说明病害数据具有明显的偏态和稀疏特征。

## 3. 实验设计

本实验分为五组。

第一组是田间指标分布分析。目的是明确每个指标有多少有效样本、缺失情况如何、数值范围是否偏态。该实验决定后续建模时是否需要谨慎解释模型精度。

第二组是遥感特征提取。围绕每个田间样点，从两期无人机影像中提取光谱特征。提取尺度包括：

| 尺度 | 含义 | 用途 |
|---|---|---|
| 点采样 | 取样点所在像元 | 对照单像元响应 |
| 1平方尺 | 与田间采样面积近似一致 | 正式分析重点尺度 |
| 0.3/0.5/1.0m | 更大空间邻域 | 分析尺度效应 |

第三组是光谱特征构建。构建的特征包括原始波段、常用植被指数、任意两波段归一化差异指数，以及两期影像之间的变化特征。

时间特征包括：

| 特征类型 | 含义 |
|---|---|
| `0418` | 4月18日单期光谱特征 |
| `0510` | 5月10日单期光谱特征 |
| `diff` | 5月10日减去4月18日 |
| `ratio` | 5月10日除以4月18日 |
| `rel` | 相对变化量 |

第四组是相关性分析。对每个田间指标和每个遥感特征计算 Pearson 相关和 Spearman 相关。Pearson 反映线性关系，Spearman 更适合判断单调关系，对非正态和异常值更稳健。本实验主要参考 Spearman 相关。

第五组是预测建模。使用交叉验证评价回归模型，包括线性回归、岭回归、Lasso、ElasticNet、PLS 和随机森林。评价指标包括 `R2`、`RMSE`、`MAE`、预测值与实测值的 Pearson 相关和 Spearman 相关。

此外，还补充了病害有无分类实验。由于连续病害指标中 0 值较多，直接预测病害数量或指数较难，因此将 `FHB_num`、`FHB_rate`、`FHB_index` 中任一指标大于 0 的样点定义为有病样点，评价遥感影像识别病害有无的能力。

## 4. 相关性分析结果

不同指标在不同尺度下的最强相关特征如下：

| 指标 | 尺度 | 最强遥感特征 | Spearman r | p值 |
|---|---|---|---:|---:|
| Flower_num | 点采样 | `diff_rpt_ND_B1_B5_mean` | 0.599 | 0.000229 |
| Flower_num | 1平方尺 | `0510_r0p172_ND_B2_B3_median` | -0.659 | 0.0000308 |
| Flower_num | 0.3/0.5/1.0m | `0510_r1_ND_B2_B3_mean` | -0.742 | 0.000000789 |
| Flower_rat | 点采样 | `diff_rpt_ND_B1_B5_mean` | 0.599 | 0.000229 |
| Flower_rat | 1平方尺 | `0510_r0p172_ND_B2_B3_median` | -0.659 | 0.0000308 |
| Flower_rat | 0.3/0.5/1.0m | `0510_r1_ND_B2_B3_mean` | -0.742 | 0.000000789 |
| FHB_num | 点采样 | `diff_rpt_CI_RE2_mean` | -0.470 | 0.000100 |
| FHB_num | 1平方尺 | `ratio_r0p172_B2_p75` | 0.463 | 0.000131 |
| FHB_num | 0.3/0.5/1.0m | `0418_r1_ND_B1_B2_mean` | 0.628 | 0.0000000352 |
| FHB_rate | 点采样 | `diff_rpt_CI_RE2_mean` | -0.463 | 0.000150 |
| FHB_rate | 1平方尺 | `rel_r0p172_B2_p75` | 0.501 | 0.0000332 |
| FHB_rate | 0.3/0.5/1.0m | `0418_r1_ND_B1_B2_mean` | 0.612 | 0.000000124 |
| FHB_index | 点采样 | `diff_rpt_CI_RE2_mean` | -0.502 | 0.0000323 |
| FHB_index | 1平方尺 | `rel_r0p172_B2_p75` | 0.480 | 0.0000795 |
| FHB_index | 0.3/0.5/1.0m | `diff_r1_CI_RE2_median` | -0.613 | 0.000000119 |

结果说明：

- 扬花数和扬花率的遥感响应较明确，1平方尺尺度下最强相关达到 0.659 左右。
- 扬花指标最敏感的特征主要来自 5月10日影像，尤其是绿光与红光组合特征 `ND_B2_B3`。
- 病害指标与红边、近红外相关变化特征有较明显关系，点采样下 `CI_RE2` 的两期差值对 `FHB_index` 表现较好。
- 大尺度缓冲区下相关性普遍更强，但它对应的地面面积明显大于 1 平方尺，因此更适合作为尺度效应分析，不能直接替代田间采样面积。

## 5. 回归预测结果

不同指标在不同尺度下的最佳回归模型如下：

| 指标 | 尺度 | 最佳模型 | n | R2 | RMSE | MAE |
|---|---|---|---:|---:|---:|---:|
| Flower_num | 点采样 | RandomForest | 33 | 0.010 | 8.801 | 7.547 |
| Flower_num | 1平方尺 | RandomForest | 33 | 0.096 | 8.406 | 6.846 |
| Flower_num | 0.3/0.5/1.0m | RandomForest | 33 | 0.482 | 6.363 | 5.170 |
| Flower_rat | 点采样 | RandomForest | 33 | 0.010 | 29.337 | 25.164 |
| Flower_rat | 1平方尺 | RandomForest | 33 | 0.096 | 28.020 | 22.813 |
| Flower_rat | 0.3/0.5/1.0m | RandomForest | 33 | 0.485 | 21.153 | 17.207 |
| FHB_num | 点采样 | Ridge | 63 | 0.166 | 4.099 | 2.609 |
| FHB_num | 1平方尺 | RandomForest | 63 | 0.062 | 4.348 | 2.703 |
| FHB_num | 0.3/0.5/1.0m | RandomForest | 63 | -0.019 | 4.532 | 2.862 |
| FHB_rate | 点采样 | RandomForest | 62 | 0.223 | 7.284 | 4.562 |
| FHB_rate | 1平方尺 | RandomForest | 62 | -0.014 | 8.320 | 5.253 |
| FHB_rate | 0.3/0.5/1.0m | RandomForest | 62 | 0.092 | 7.873 | 5.164 |
| FHB_index | 点采样 | Ridge | 62 | 0.270 | 0.065 | 0.040 |
| FHB_index | 1平方尺 | ElasticNet | 62 | 0.063 | 0.073 | 0.042 |
| FHB_index | 0.3/0.5/1.0m | RandomForest | 62 | 0.205 | 0.068 | 0.039 |

结果说明：

- 回归模型整体属于探索性预测水平，不适合表述为高精度反演。
- 扬花指标在 1平方尺尺度下相关性较强，但交叉验证回归精度不高，说明样本量偏少、样本分布较宽，模型泛化能力有限。
- 扬花指标在 1m 缓冲尺度下回归 R2 接近 0.48，说明扩大空间邻域后信号更稳定，但该尺度已明显大于田间采样面积。
- 病害指标中 `FHB_index` 的点采样预测表现最好，R2 为 0.270，说明红边相关变化特征对病害指数有一定预测价值。
- `FHB_num` 和 `FHB_rate` 的连续值预测能力较弱，可能与病害发生斑块化、0值样本较多、田间调查误差和样点面积较小有关。

## 6. 病害有无分类结果

将病害指标转为有病和无病后，分类结果如下：

| 尺度 | 模型 | n | 有病样本 | 无病样本 | Balanced accuracy | ROC AUC | F1 |
|---|---|---:|---:|---:|---:|---:|---:|
| 点采样 | LogisticRegression | 63 | 34 | 29 | 0.649 | 0.702 | 0.676 |
| 点采样 | RandomForestClassifier | 63 | 34 | 29 | 0.634 | 0.718 | 0.657 |
| 1平方尺 | LogisticRegression | 63 | 34 | 29 | 0.698 | 0.744 | 0.716 |
| 1平方尺 | RandomForestClassifier | 63 | 34 | 29 | 0.710 | 0.683 | 0.743 |

结果说明：

- 病害有无分类的表现比连续病害数值回归更稳定。
- 1平方尺尺度下的分类结果最好，随机森林的 Balanced accuracy 为 0.710，F1 为 0.743；逻辑回归的 ROC AUC 为 0.744。
- 这说明当前数据更适合先做病害风险识别或有无判断，再尝试病害程度定量预测。

## 7. 时间特征与特征类型解释

从时间特征看：

- 扬花指标更依赖 5月10日单期影像，说明扬花状态与接近调查时期的冠层颜色和光谱差异关系更直接。
- 病害指标对两期变化特征较敏感，尤其是 `diff`、`ratio`、`rel` 这类时间变化特征。
- 病害指数 `FHB_index` 在点采样下最优特征是 `diff_rpt_CI_RE2_mean`，表明红边2相关指数的阶段变化具有解释价值。

从特征类型看：

- 扬花指标的重要特征多为绿光、红光相关组合指数。
- 病害指标的重要特征多涉及蓝光、绿光、红边和近红外的组合或变化。
- 单一原始波段可以提供信息，但组合指数和时间变化特征通常更容易解释冠层生理和病害变化。

## 8. 预测制图结果

基于已有最佳模型，已经生成两类预测图：

- `FHB_index` 预测图：采用点采样尺度下表现最好的 Ridge 模型。交叉验证 R2 为 0.270，适合表达病害指数空间趋势。
- `Flower_rat` 预测图：采用 1平方尺尺度下的 RandomForest 模型。交叉验证 R2 为 0.096，适合表达扬花率空间趋势，但精度不宜过度强调。

这些图可以用于 PPT 或文章中的空间展示，但应表述为趋势图、风险图或相对高低分布图，不宜表述为精确预测图。

## 9. 总体结论

本实验说明，无人机多光谱影像与扬花及病害指标之间存在可观测的遥感响应关系。

对扬花指标而言，5月10日影像中的绿光和红光组合特征与扬花数、扬花率关系最明显。1平方尺尺度下相关性较强，说明遥感影像能够捕捉扬花造成的冠层光谱差异。但是由于扬花有效样本只有 33 个，回归预测精度仍然有限。

对病害指标而言，红边、近红外以及两期变化特征更重要。病害指数 `FHB_index` 的点采样回归结果相对最好，说明遥感影像对病害严重度具有一定响应。但病害连续值预测仍受 0 值多、样本分布偏态和病害空间斑块化影响。

从建模角度看，当前数据更适合支撑以下结论：

- 可以分析遥感特征与扬花及病害指标之间的响应关系。
- 可以比较不同采样尺度对相关性和预测效果的影响。
- 可以生成扬花率和病害指数的空间趋势图。
- 病害有无识别比连续病害程度预测更稳定。

当前数据不适合直接支撑“高精度定量反演”的强结论。更稳妥的论文定位是：基于无人机多光谱影像的扬花与病害指标遥感响应、尺度效应及空间趋势预测。

## 10. 后续建议

如果要进一步提高论文和应用价值，建议补充以下实验或数据：

- 增加扬花样本数量，尤其是低扬花率和中等扬花率样本。
- 增加病害严重样本，改善病害指数分布偏态问题。
- 在多个日期重复采样，构建更完整的时序变化特征。
- 将病害预测分为两步：先判断有无病害，再对有病样点预测病害程度。
- 对预测图增加不确定性说明，避免把趋势预测解释为精确测量。

## 11. 下一步需要的材料与审计重点

下一步如果要继续推进论文或正式技术报告，建议先整理一套完整的原始数据、过程数据和建模材料。这样可以先完成一次数据与建模流程审计，判断当前相关性和模型 `R2` 是否可信，再决定哪些结果可以进入正文，哪些结果只能作为补充实验。

这里需要区分两类结果文件。第一类是每一套采样尺度单独生成的基础结果表，例如点采样、1平方尺采样、多尺度缓冲区各自的 `correlation_summary.csv` 和 `model_metrics.csv`。第二类是后续补充实验生成的综合汇总表，例如 `best_correlation_by_scale.csv`、`best_model_by_scale.csv` 和 `model_comparison_all.csv`。审计时两类文件都应提供，并且最好同时提供生成这些表的代码和上游数据，避免只审查已经筛选过的“最优结果”。

建议优先准备以下材料：

| 材料 | 用途 |
|---|---|
| `field_samples_clean.csv` | 检查田间调查指标的清洗结果、缺失值、异常值和样本量 |
| `sample_rs_features.csv` | 检查每个样点提取到的遥感特征是否合理 |
| `all_features_targets.csv` | 检查遥感特征和田间指标是否正确合并 |
| 各尺度的 `correlation_summary.csv` | 审核每套采样尺度下的完整相关性分析结果，判断显著特征是否稳定 |
| 各尺度的 `model_metrics.csv` | 审核每套采样尺度下的模型交叉验证结果，判断预测精度是否可信 |
| 各尺度的 `feature_importance.csv` | 解释模型主要依赖哪些波段、指数或时间变化特征 |
| 补充实验汇总表 | 审核不同尺度、时间特征和模型之间的综合比较结果 |
| 当前建模代码 | 检查特征筛选、交叉验证、模型训练和评价流程是否存在数据泄漏 |
| 样点或试验小区分布图 | 判断样点空间分布是否均匀，是否存在空间聚集或边界误差 |
| 传感器型号、飞行高度、拍摄时间、田间调查日期 | 用于说明影像质量、空间分辨率、时间匹配关系和实验可重复性 |
| 品种、处理、重复区及样点之间的从属关系 | 用于判断样点是否相互独立，是否需要按处理、品种或重复区进行分组验证 |

审计时重点检查以下问题：

- 田间样点与遥感影像是否空间对齐。
- 确认 `Flower_num` 与 `Flower_rat` 是否仅为固定分母下的线性换算，避免将同一目标重复汇报。
- 明确“1平方尺”的实际面积；若为 1 ft²，应统一写为“1 ft²（0.0929 m²，等面积圆半径约 0.172 m）”。
- 1平方尺采样面积与遥感特征提取范围是否匹配。
- 检查4月18日影像是否完成辐射定标；若未定标，应评估 `diff`、`ratio` 和 `rel` 跨期特征是否受光照与曝光差异影响。
- 缺失值和 0 值是否处理合理。
- 检查大量波段组合、日期、尺度和统计量搜索是否进行 FDR 多重检验校正。
- 检查各尺度是否分别汇报，避免将 0.3、0.5 和 1.0 m 中的最优结果合并为一个结果。
- 检查相邻样点缓冲区是否重叠，尤其是 0.5 和 1.0 m 尺度，避免训练样本与测试样本共享影像像元。
- 同一个试验小区或重复区内的样点是否被同时用于训练和验证。
- 特征筛选是否只在训练折内完成，避免把测试集信息提前用于选特征。
- 标准化、缺失值填补、特征筛选和超参数优化是否全部在训练折内完成。
- 根据试验小区、品种、处理或空间分区，评估是否需要 Group CV 或空间交叉验证。
- 对最优相关系数和模型性能进行置换检验、Bootstrap 置信区间及特征稳定性分析。
- 当前模型的 `R2` 是否来自严格交叉验证，而不是训练集拟合结果。
- 预测图是否只作为空间趋势表达，而不是被解释为精确地面实测值。

现阶段最重要的不是立即写正文，而是先把验证方法做严谨。方法确定后，论文故事会比较清楚：用无人机多光谱影像识别扬花和病害的光谱响应，比较不同采样尺度和时间变化特征的作用，并在谨慎验证的基础上生成空间趋势预测图。

## 12. 对应结果文件

本实验整理主要依据两类结果文件。基础结果文件是每套采样尺度分别生成的完整表，补充实验文件是在基础结果上继续汇总、比较和筛选得到的派生表。

基础结果文件包括：

| 文件 | 内容 |
|---|---|
| `field_samples_clean.csv` | 清洗后的田间样点指标 |
| `sample_rs_features.csv` | 每个样点提取的遥感特征 |
| `all_features_targets.csv` | 遥感特征与田间指标合并后的建模总表 |
| `correlation_summary.csv` | 单一尺度下的完整相关性结果 |
| `model_metrics.csv` | 单一尺度下的完整建模评价结果 |
| `feature_importance.csv` | 单一尺度下的模型特征重要性 |

补充实验汇总文件包括：

| 文件 | 内容 |
|---|---|
| `target_distribution_summary.csv` | 田间指标分布统计 |
| `best_correlation_by_scale.csv` | 各尺度最强相关特征 |
| `scale_comparison_top_correlations.csv` | 各尺度前若干相关特征 |
| `best_model_by_scale.csv` | 各尺度最佳回归模型 |
| `model_comparison_all.csv` | 全部回归模型结果 |
| `temporal_feature_summary.csv` | 时间特征表现汇总 |
| `feature_group_summary.csv` | 特征类别表现汇总 |
| `disease_classification_metrics.csv` | 病害有无分类结果 |
